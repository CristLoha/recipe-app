import 'package:flutter/material.dart';

class UploadFragment extends StatelessWidget {
  const UploadFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Upload Fragment',
        style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
      ),
    );
  }
}
